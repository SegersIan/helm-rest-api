const getReleases = require('./get-releases');
const getVersion = require('./get-version-info');
const installRelease = require('./install-release');

module.exports = {
    getReleases,
    getVersion,
    installRelease
}